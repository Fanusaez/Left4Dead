#ifndef INSTRUCTIONS_H
#define INSTRUCTIONS_H

enum Instructions {
    CREATE,
    JOIN,
    GAME_MODE,
    SOLDIER_TYPE,
    GAME_LIST,
    START,
    MOVE,
    RELOAD,
    SHOOT,
    GRANEDE
};
#endif
