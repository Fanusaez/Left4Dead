#ifndef INSTRUCTIONSTYPE_H
#define INSTRUCTIONSTYPE_H

enum InstructionsType {
    IDLE,
    CREATE,
    JOIN,
    GAME_MODE,
    SOLDIER_TYPE,
    GAME_LIST,
    START,
    MOVE,
    RELOAD,
    SHOOT,
    GRANEDE
};
#endif
